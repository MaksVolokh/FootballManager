@using FootballManagerDAL.Entities
@model IEnumerable<Chat>

<div class="chat-body">
    @foreach (var chat in Model)
    {
        <a class="room-button" asp-controller="Chat" asp-action="JoinRoomAsync" asp-route-id="@chat.Id">
            @chat.Name
        </a>
    }
</div>